/* import { connection } from "../dataBase";
import { classes } from "../../types";

export const createClass = async (
  class_name: string,
  module: number
): Promise<any> => {
  await connection("classes").insert({ class_name, module });
};
 */