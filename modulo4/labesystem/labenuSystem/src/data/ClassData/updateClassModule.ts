/* import { connection } from "../dataBase";

export const updateClassModule = async (
  id: number,
  module: number
): Promise<any> => {
  await connection("classes").update({ module }).where("id", `${id}`);
};
 */